on: 
  push:

jobs:
  my_first_job:
    name: first job
    runs-on: self-hosted
    steps:
      - run: echo "my first job ran successful"
      
  my_second_job:
    name: second job
    needs: my_first_job
    runs-on: self-hosted
    
